# Edge Function 配置
# 关闭网关层 JWT 校验，避免与新版 Auth 签名（如 ES256）导致的 401 Invalid JWT。
# 认证仍在函数内通过 /auth/v1/user 调用完成。
[functions.analyze]
verify_jwt = false

[functions.paddle-webhook]
verify_jwt = false

# create-order 使用默认 verify_jwt=true，由 Supabase 网关验证 JWT
# 函数内部也会再次验证 token
